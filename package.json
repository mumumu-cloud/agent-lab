{
  "name": "agent-lab",
  "version": "0.1.0",
  "private": true,
  "description": "A small starter lab for experimenting with agent-style request/response flows.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
